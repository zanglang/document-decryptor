# Example only. Replace the placeholder values and apply with a real
# password before use. Do not commit real secrets to version control.
apiVersion: v1
kind: Secret
metadata:
  name: document-decryptor-config
type: Opaque
stringData:
  patterns.json: |
    {
      "company-payslip": {
        "patterns": ["payslip"],
        "password": "REPLACE_ME"
      },
      "example-bank": {
        "patterns": ["example-bank.com"],
        "password": "REPLACE_ME"
      },
      "example-bank-credit-card": {
        "patterns": ["credit card statement"],
        "password": "REPLACE_ME"
      }
    }
