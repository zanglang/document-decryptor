# Example only. Replace the placeholder values and apply with a real
# password before use. Do not commit real secrets to version control.
apiVersion: v1
kind: Secret
metadata:
  name: document-decryptor-config
type: Opaque
stringData:
  patterns.json: |
    {
      "payslip": {
        "name": "company-payslip",
        "password": "REPLACE_ME"
      },
      "example-bank.com": {
        "name": "example-bank",
        "password": "REPLACE_ME"
      },
      "credit card statement": {
        "name": "example-bank-credit-card",
        "password": "REPLACE_ME"
      }
    }
