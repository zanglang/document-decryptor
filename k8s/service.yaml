apiVersion: v1
kind: Service
metadata:
  name: document-decryptor
  labels:
    app: document-decryptor
spec:
  type: ClusterIP
  selector:
    app: document-decryptor
  ports:
    - name: http
      port: 8080
      targetPort: http
